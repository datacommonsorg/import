main:
  params: [args]
  steps:
    - init:
        assign:
          - lock_timeout: 82800 # 23 hours
          - wait_period: 300 # seconds
          - project_id: '${sys.get_env("PROJECT_ID")}'
          - dataflow_job_name: ${"ingestion-job-" + string(int(sys.now()))}
          - dataflow_gcs_path: '${sys.get_env("DATAFLOW_TEMPLATE_PATH")}'
          - location: '${sys.get_env("LOCATION")}'
          - spanner_project_id: '${sys.get_env("SPANNER_PROJECT_ID")}'
          - spanner_instance_id: '${sys.get_env("SPANNER_INSTANCE_ID")}'
          - spanner_database_id: '${sys.get_env("SPANNER_DATABASE_ID")}'
          - helper_url: ${"https://ingestion-helper-service-" + sys.get_env("PROJECT_NUMBER") + "." + location + ".run.app"}
          - import_list: ${default(map.get(args, "importList"), [])}
          - skip_aggregation: ${default(map.get(args, "skipAggregation"), true)}
          - skip_embedding: ${sys.get_env("ENABLE_EMBEDDINGS") != "true"}
          - dataflow_job_id: null
          - execution_error: null
    - acquire_ingestion_lock:
        try:
          call: http.post
          args:
            url: ${helper_url + "/database/lock/acquire"}
            auth:
              type: OIDC
            body:
              workflowId: '${sys.get_env("GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID")}'
              timeout: ${lock_timeout}
          result: lock_status
        retry:
          predicate: ${http.default_retry_predicate}
          max_retries: 20
          backoff:
            initial_delay: 300
            max_delay: 600
            multiplier: 2
    - process_ingestion:
        try:
          steps:
            - get_import_info:
                call: http.post
                args:
                  url: ${helper_url + "/imports/info"}
                  auth:
                    type: OIDC
                  body:
                    importList: ${import_list}
                result: import_info
            - run_ingestion_job:
                call: run_dataflow_job
                args:
                  import_list: '${json.encode_to_string(import_info.body)}'
                  project_id: ${project_id}
                  job_name: ${dataflow_job_name}
                  template_gcs_path: ${dataflow_gcs_path}
                  location: ${location}
                  spanner_project_id: ${spanner_project_id}
                  spanner_instance_id: ${spanner_instance_id}
                  spanner_database_id: ${spanner_database_id}
                  wait_period: ${wait_period}
                  helper_url: ${helper_url}
                  workflow_id: '${sys.get_env("GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID")}'
                result: dataflow_job_id
            - run_parallel_aggregation_and_embedding:
                parallel:
                  branches:
                    - aggregation_branch:
                        steps:
                          - check_skip_aggregation:
                              switch:
                                - condition: ${skip_aggregation}
                                  next: end_aggregation_branch
                          - run_aggregation_job:
                              call: run_aggregation_job
                              args:
                                import_list: ${import_info.body}
                                helper_url: ${helper_url}
                          - end_aggregation_branch:
                              assign:
                                - dummy_agg: null
                    - embedding_branch:
                        steps:
                          - check_skip_embedding:
                              switch:
                                - condition: ${skip_embedding}
                                  next: end_embedding_branch
                          - run_embedding_job:
                              call: run_embedding_job
                              args:
                                helper_url: ${helper_url}
                          - end_embedding_branch:
                              assign:
                                - dummy_emb: null
            - update_ingestion_status:
                call: http.post
                args:
                  url: ${helper_url + "/imports/ingestion-status"}
                  auth:
                    type: OIDC
                  body:
                    workflowId: '${sys.get_env("GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID")}'
                    jobId: '${dataflow_job_id}'
                    importList: '${import_info.body}'
                    status: 'SUCCESS'
                result: function_response
        except:
          as: e
          steps:
            - record_failure:
                call: http.post
                args:
                  url: ${helper_url + "/imports/ingestion-status"}
                  auth:
                    type: OIDC
                  body:
                    workflowId: '${sys.get_env("GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID")}'
                    jobId: ${default(dataflow_job_id, default(map.get(e, "job_id"), "N/A"))}
                    importList: '${import_info.body}'
                    status: 'RETRY'
                result: retry_response
            - capture_error:
                assign:
                  - execution_error: ${e}
    - release_ingestion_lock:
        call: http.post
        args:
          url: ${helper_url + "/database/lock/release"}
          auth:
            type: OIDC
          body:
            workflowId: '${sys.get_env("GOOGLE_CLOUD_WORKFLOW_EXECUTION_ID")}'
        result: function_response
    - fail_workflow:
        switch:
          - condition: ${execution_error != null}
            raise: ${execution_error}
    - return_import_info:
        return: '${import_info.body}'

# This sub-workflow runs aggregation jobs and waits for them to complete.
run_aggregation_job:
  params: [import_list, helper_url]
  steps:
    - run_aggregation:
        call: http.post
        args:
          url: ${helper_url + "/aggregation/run"}
          timeout: 300
          auth:
            type: OIDC
          body:
            importList: ${import_list}
        result: aggregation_response
    - check_aggregation_status_loop:
        steps:
          - wait_for_aggregation:
              call: sys.sleep
              args:
                seconds: 300
          - check_aggregation_status:
              call: http.post
              args:
                url: ${helper_url + "/aggregation/status"}
                auth:
                  type: OIDC
                body:
                  jobIds: ${aggregation_response.body.jobIds}
              result: aggregation_status_response
          - evaluate_aggregation_status:
              switch:
                - condition: ${aggregation_status_response.body.status == "DONE"}
                  return: 'OK'
                - condition: ${aggregation_status_response.body.status == "FAILED"}
                  raise: ${aggregation_status_response.body.error}
              next: check_aggregation_status_loop

# This sub-workflow runs embedding jobs and waits for them to complete.
run_embedding_job:
  params: [helper_url]
  steps:
    - run_embedding:
        call: http.post
        args:
          url: ${helper_url + "/embeddings/ingest"}
          timeout: 3600
          auth:
            type: OIDC
          body:
            enableEmbeddings: true
        result: embedding_response

# This sub-workflow launches a Dataflow job and waits for it to complete.
run_dataflow_job:
  params: [import_list, project_id, job_name, template_gcs_path, location, spanner_project_id, spanner_instance_id, spanner_database_id, wait_period, helper_url, workflow_id]
  steps:
    - init:
        assign:
          - jobName: ${job_name}
          - machineType: 'n2-highmem-8'
          - numWorkers: 3
    - log_imports:
        call: sys.log
        args:
          text: '${"Dataflow job: " + jobName + " Import list: " + import_list}'
          severity: INFO 
    - check_if_empty:
        switch:
          - condition: ${import_list == "[]"}
            return: ''
    - launch_dataflow_job:
        call: googleapis.dataflow.v1b3.projects.locations.flexTemplates.launch
        args:
          projectId: '${project_id}'
          location: '${location}'
          body:
            launchParameter:
              containerSpecGcsPath: '${template_gcs_path}'
              jobName: '${jobName}'
              parameters:
                importList: '${import_list}'
                projectId: '${spanner_project_id}'
                spannerInstanceId: '${spanner_instance_id}'
                spannerDatabaseId: '${spanner_database_id}'
                # Revert this once switched to the new DB.
                spannerObservationTableName: 'Observation_final_v3'
                spannerTimeSeriesTableName: 'TimeSeries_final_v3'
              environment:
                numWorkers: ${numWorkers}
                machineType: ${machineType}
        result: launch_result
    - wait_for_job_completion:
        call: sys.sleep
        args:
          seconds: ${wait_period}
        next: check_job_status
    - check_job_status:
        call: googleapis.dataflow.v1b3.projects.locations.jobs.get
        args:
          projectId: '${project_id}'
          location: '${location}'
          jobId: '${launch_result.job.id}'
          view: 'JOB_VIEW_SUMMARY'
        result: job_status
        next: check_if_done
    - check_if_done:
        switch:
          - condition: ${job_status.currentState == "JOB_STATE_DONE"}
            return: ${launch_result.job.id}
          - condition: ${job_status.currentState == "JOB_STATE_FAILED" or job_status.currentState == "JOB_STATE_CANCELLED"}
            raise:
              message: '${jobName + " dataflow job failed with status: " + job_status.currentState}'
              code: 500
              job_id: ${launch_result.job.id}
        next: wait_for_job_completion
